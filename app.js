setTimeout(()=>{const x=document.querySelector('#loader');x.style.opacity='0';setTimeout(()=>x.remove(),550)},1350);
const menu=document.querySelector('.menu'),nav=document.querySelector('nav');menu.onclick=()=>nav.classList.toggle('open');document.querySelectorAll('nav a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));
const command='npm run build:future';let i=0;function type(){document.querySelector('#cmd').textContent=command.slice(0,i++);if(i<=command.length)setTimeout(type,70)}setTimeout(type,800);
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));
const c=document.querySelector('#matrix'),x=c.getContext('2d');let w,h,cols,drops;function resize(){w=c.width=innerWidth;h=c.height=innerHeight;cols=Math.floor(w/15);drops=Array(cols).fill(1)}resize();addEventListener('resize',resize);const chars='01{}[]<>/$#@';setInterval(()=>{x.clearRect(0,0,w,h);x.font='11px monospace';for(let i=0;i<cols;i++){x.fillText(chars[(Math.random()*chars.length)|0],i*15,drops[i]*15);if(drops[i]*15>h&&Math.random()>.975)drops[i]=0;drops[i]++}},75);